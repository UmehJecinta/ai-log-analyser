apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-log-analyser-monitor
  namespace: default
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: backend
  endpoints:
    - port: "80"
      path: /metrics
      interval: 30s